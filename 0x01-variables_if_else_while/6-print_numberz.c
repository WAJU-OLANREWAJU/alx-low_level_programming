#include <stdio.h>
/**
 * main -My program that prints all single digit numbers of base 10
 *
 * Return: 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
{
		putchar("%d" ; n)
}

	putchar(\n);
	return (0);
}
